import React from "react";

import MainContainer from "../../Shared/components/mainContainer";
import styles from "./builds.module.css";

const Builds = () => {
  return (
    <MainContainer>
      <div className={styles}>builds</div>
    </MainContainer>
  );
};

export default Builds;
